# binaries
/gcsa
/validate
/bench_repetition
/compare_algos
/ilp_baseline
/simulate_repeats
/simulate_random
/simulate_pangenome
/dump_graph
/find_minimal_gap
*.o
*.lp
a.out

# optional vendored libdivsufsort (not required; SA-IS is header-only)
external/

# local test data / scratch
*.fasta
/out
.DS_Store

# LaTeX build artifacts (only the .tex and .pdf are tracked)
*.aux
*.log
*.out
*.toc
*.synctex.gz

# solver output / one-off run scratch
*.sol
/outp
/table
/ilp_____lp

# benchmark suite output (run artifact, not source -- see bench/README.md)
/bench/results/

# per-machine paths for the benchmark suite -- never commit a real
# filesystem path; see bench/config.local.py.example
/bench/config.local.py
